/**
 * Enumerations shared by Pokédex systems across the engine.
 */

/**
 * Match the Game Boy's Pokédex sorting modes by their numeric IDs.
 */
export enum DexMode {
  NEW = 0,
  OLD = 1,
  ABC = 2,
  UNOWN = 3,
}
